blueprint:
  name: Ручное освещение
  description: >
    # 🪄 Ручное освещение
    
    **Версия: 1.3**
    👋 Попрощайтесь с забытыми включенными светильниками! Освещение с автоматическим отключением и сбросом по датчику движения. 🪄💡

    <details>
    <summary><b>Процесс автоматизации:</b> 👈</summary>


      - **Триггер:**
        - При ручном включении объекта он автоматически выключится через заданную временную задержку.

      - **Опция синхронизации триггеров:**
        - При использовании нескольких объектов при изменении состояния одного из них (ручное включение или выключение) все объекты автоматически синхронизируют свое состояние.

      - **Опция датчика движения:**
        - При ручном включении света и обнаружении движения датчиком таймер будет приостановлен.
        - После прекращения движения таймер сбросится и начнется заново.
        - Если таймер не используется, временная задержка перезапустится после прекращения движения.

      - **Период ожидания:**
        - При использовании датчика движения, если свет выключается автоматически, короткий период ожидания позволяет автоматически включить его снова при обнаружении движения.
        - После окончания периода ожидания свет должен быть включен вручную.
        - **Примечание:** Это не применяется, если свет выключен вручную - ручное выключение всегда имеет приоритет.

      - **Обход (Bypass):**
        - Используйте опцию обхода для ручного отключения автоматизации, что дает вам полный контроль.

      - **Поведение при перезагрузке HA:**
        - С помощником-таймером:
          - Автоматизация возобновит работу после перезагрузки Home Assistant, сохраняя состояние таймера.
        - Без помощника-таймера:
          - После перезагрузки Home Assistant автоматизация попытается сброситься, перезапуская временную задержку с начала.
    </details>


    Нужна помощь?
    
    ЧАВО: [Нажмите здесь](https://community.home-assistant.io/t/688273/2)
    
    Поддержка сообщества, включая обновления: [Нажмите здесь](https://community.home-assistant.io/t/688273/1)


    Обязательные поля = *
  domain: automation
  input:
    trigger:
      name: "Триггер *"
      icon: mdi:cog-outline
      collapsed: true
      input:
        trigger_input:
          name: Триггер - Светильники и выключатели *
          description: >
            Выберите объекты, которые будут автоматически выключаться после настроенной временной задержки.


            **Примечание** — Все выбранные объекты должны поддерживать состояния ВКЛ и ВЫКЛ.
          default: []
          selector:
            entity:
              filter:
                domain:
                  - light
                  - switch
              multiple: true
        time_delay:
          name: Временная задержка
          description: >
            Установите, как долго светильники и выключатели остаются включенными перед автоматическим выключением.


            При использовании датчика движения таймер сбрасывается при обнаружении движения.
            Когда движение не обнаружено и задержка истекла, светильники и выключатели выключаются.
          default:
            days: 0
            hours: 0
            minutes: 5
            seconds: 0
          selector:
            duration:
        include_timer_helper:
          name: Использовать помощник-таймер (опционально)
          description: >
            Включите эту опцию, если хотите использовать помощник-таймер.


            **Примечание** — При создании помощника-таймера отметьте галочку `Восстановить`, чтобы он сохранялся после перезагрузки Home Assistant.
            Не устанавливайте длительность в самом помощнике-таймере; время контролируется настройками ниже.


            Для получения дополнительной информации о создании помощника-таймера [нажмите здесь](https://community.home-assistant.io/t/688273/4).
          default: disable_timer_helper
          selector:
            select:
              options:
                - label: Включить опцию помощника-таймера
                  value: "enable_timer_helper"
                - label: Отключить опцию помощника-таймера
                  value: "disable_timer_helper"
        timer_helper:
          name: Помощник-таймер
          description: >
            Выберите помощника-таймера для использования, когда опция помощника-таймера включена выше.
          default: []
          selector:
            entity:
              filter:
                domain:
                  - timer
        include_trigger_sync:
          name: Использовать опцию синхронизации (опционально)
          description: >
            Включите эту опцию для синхронизации состояний ВКЛ/ВЫКЛ всех выбранных объектов.
            Если выбрано более одного объекта, они будут автоматически включаться и выключаться синхронно.
          default: disable_trigger_sync
          selector:
            select:
              options:
                - label: Включить опцию синхронизации
                  value: "enable_trigger_sync"
                - label: Отключить опцию синхронизации
                  value: "disable_trigger_sync"
    motion:
      name: "Датчик движения"
      icon: mdi:motion-sensor
      collapsed: true
      input:

        include_motion_trigger:
          name: Использовать опцию датчика движения (опционально)
          description: >
            Включите эту опцию для использования датчика движения.
            При использовании помощника-таймера или встроенной временной задержки в Home Assistant, таймер или задержка будет приостанавливаться при обнаружении движения и сбрасываться после прекращения движения.
          default: disable_motion_trigger
          selector:
            select:
              options:
                - label: Включить опцию датчика движения
                  value: "enable_motion_trigger"
                - label: Отключить опцию датчика движения
                  value: "disable_motion_trigger"
        motion_trigger:
          name: Датчик движения - Бинарные сенсоры
          description: >
            Выберите один датчик движения или бинарный сенсор.
            Если необходимо использовать несколько сенсоров, сначала сгруппируйте их с помощью помощника-группы.
            Это обеспечивает более плавное выполнение автоматизации и помогает предотвратить конфликты.


            Для получения дополнительной информации о группировке триггерных сенсоров [нажмите здесь](https://community.home-assistant.io/t/688273/3).
          default: []
          selector:
            entity:
              filter:
                domain:
                  - binary_sensor
    grace_period:
      name: "Период ожидания"
      icon: mdi:timer-outline
      collapsed: true
      input:
        include_grace_period:
          name: Использовать опцию периода ожидания (опционально)
          description: >
            Включите короткий период ожидания после выключения света из-за отсутствия движения.
            Если движение обнаружено в течение этого периода, свет автоматически включится снова.
            После окончания периода ожидания свет необходимо включать вручную.


            **Примечание** - Эта опция работает только при включенной опции датчика движения.
          default: disable_grace_period
          selector:
            select:
              options:
                - label: Включить опцию периода ожидания
                  value: "enable_grace_period"
                - label: Отключить опцию периода ожидания
                  value: "disable_grace_period"
        grace_period_timer_helper:
          name: Помощник-таймер периода ожидания
          description: >
            Выберите помощника-таймера для использования, если вы выбрали использование периода ожидания выше.


            **Примечание** — При создании помощника-таймера отметьте галочку `Восстановить`, чтобы он сохранялся после перезагрузки Home Assistant.
            Не устанавливайте длительность в самом помощнике-таймере; время контролируется настройками ниже.


            При использовании таймера в опции **Помощник-таймер** выше, вы должны использовать отдельный независимый таймер здесь.


            Для получения дополнительной информации о помощнике-таймере [Нажмите здесь](https://community.home-assistant.io/t/688273/4)
          default: []
          selector:
            entity:
              filter:
                domain:
                  - timer
        grace_period_time_delay:
          name: Временная задержка периода ожидания
          description: >
            Установите, как долго длится период ожидания после выключения света из-за отсутствия движения.
            В течение этого времени, если движение обнаружено, свет автоматически включится снова.
            После окончания периода ожидания свет необходимо включать вручную.
          default:
            days: 0
            hours: 0
            minutes: 5
            seconds: 0
          selector:
            duration:
        saved_lights_input:
          name: Текстовый помощник для сохранения включенных светильников
          description: >
            При использовании более одного триггерного объекта без опции **Синхронизация**,
            выберите объект `текстовый помощник` для хранения списка светильников, которые в данный момент включены.


            Если вы используете только один триггерный объект или опцию **Синхронизация**, это поле можно оставить пустым.
          default: []
          selector:
            entity:
              filter:
                domain: input_text
    bypass_settings:
      name: "Обход (Bypass)"
      icon: mdi:cog-pause-outline
      collapsed: true
      input:
        include_bypass:
          name: Использовать опции обхода (опционально)
          description: >
            Выберите, хотите ли вы включить опцию.
            Каждая опция определяет поведение ваших светильников при включении переключателя обхода:

              1 - Включает светильники
              2 - Выключает светильники
              3 - Сохраняет текущее состояние светильников

            Если вы включаете опцию, убедитесь, что указали соответствующий переключатель обхода в поле ниже.


            Для получения дополнительной информации об использовании обхода [Нажмите здесь](https://community.home-assistant.io/t/688273/89)
          default: []
          selector:
            select:
              options:
                - label: 1 - Включить обход - Включить светильники
                  value: "bypass_enabled_turn_on"
                - label: 2 - Включить обход - Выключить светильники
                  value: "bypass_enabled_turn_off"
                - label: 3 - Включить обход - Сохранить текущее состояние светильников
                  value: "bypass_enabled_stop"
              multiple: true
        bypass_lights_on:
          name: Переключатель обхода - Включить светильники
          description: >
            Выберите переключатели, которые включат ваши светильники, обойдут триггерный сенсор и позволят вашим светильникам работать в обычном режиме.
            Обратите внимание, что объект не может быть включен в выбор "Триггер - Светильники и выключатели".
          default: []
          selector:
            entity:
              multiple: true
        bypass_lights_off:
          name: Переключатель обхода - Выключить светильники
          description: >
            Выберите переключатели, которые выключат ваши светильники, обойдут триггерный сенсор и позволят вашим светильникам работать в обычном режиме.
            Обратите внимание, что объект не может быть включен в выбор "Триггер - Светильники и выключатели".
          default: []
          selector:
            entity:
              multiple: true
        bypass_lights_stop:
          name: Переключатель обхода - Сохранить текущее состояние светильников
          description: >
            Выберите переключатели, которые сохранят текущее состояние ваших светильников, обойдут триггерный сенсор и позволят вашим светильникам работать в обычном режиме.
            Обратите внимание, что объект не может быть включен в выбор "Триггер - Светильники и выключатели".
          default: []
          selector:
            entity:
              multiple: true
        bypass_time_delay:
          name: Обход - Временная задержка
          description: >
            Используется только в двух конкретных сценариях обхода, когда ваши светильники включены.

              1. Когда выбрана опция 2 выше "Включить обход - Выключить светильники" и обход включен.
              2. Когда все обходы выключены, а светильники остаются включенными.

            В обоих случаях автоматизация выключит светильники после настроенной временной задержки.
          default: 0
          selector:
            number:
              min: 0
              max: 10
              step: 0.25
              unit_of_measurement: минуты
        include_bypass_auto_off:
          name: Использовать опцию автоматического отключения обхода (опционально)
          description: >
            Включите эту опцию, если хотите, чтобы обход автоматически отключался после установленной временной задержки.
            Вы можете выбрать, к какой опции обхода применится автоматическое отключение:

              1 - Включает светильники
              2 - Выключает светильники
              3 - Сохраняет текущее состояние светильников

            Каждая опция соответствует одной из опций обхода выше.
          default: []
          selector:
            select:
              options:
                - label: A - Включить автоматическое отключение для опции обхода 1 - Включить светильники
                  value: "bypass_auto_off_enabled_on"
                - label: B - Включить автоматическое отключение для опции обхода 2 - Выключить светильники
                  value: "bypass_auto_off_enabled_off"
                - label: C - Включить автоматическое отключение для опции обхода 3 - Сохранить текущее состояние светильников
                  value: "bypass_auto_off_enabled_stop"
              multiple: true
        bypass_auto_off_delay:
          name: Автоматическое отключение обхода - Временная задержка
          description: >
            Установите временную задержку автоматического отключения обхода.
            Временная задержка начинается с момента включения последнего обхода.
            Если включено несколько обходов, выключение одного при оставшемся включенном другом также сбросит временную задержку.
          default: 60
          selector:
            number:
              min: 1
              max: 240
              step: 1
              unit_of_measurement: минуты

mode: restart
max_exceeded: silent

variables:
  trigger_input: !input trigger_input
  time_delay: !input time_delay
  include_timer_helper: !input include_timer_helper
  timer_helper: !input timer_helper
  include_trigger_sync: !input include_trigger_sync
  include_motion_trigger: !input include_motion_trigger
  motion_trigger: !input motion_trigger
  include_grace_period: !input include_grace_period
  grace_period_timer_helper: !input grace_period_timer_helper
  grace_period_time_delay: !input grace_period_time_delay
  saved_lights_input: !input saved_lights_input
  include_bypass: !input include_bypass
  bypass_lights_on: !input bypass_lights_on
  bypass_lights_off: !input bypass_lights_off
  bypass_lights_stop: !input bypass_lights_stop
  bypass_time_delay: !input bypass_time_delay
  include_bypass_auto_off: !input include_bypass_auto_off
  bypass_auto_off_delay: !input bypass_auto_off_delay

  trigger_entities_on: >
    {{ expand(trigger_input)
        | selectattr('state', 'eq', 'on')
        | map(attribute='entity_id')
        | list }}

  trigger_entities_off: >
    {{ expand(trigger_input)
        | selectattr('state', 'eq', 'off')
        | map(attribute='entity_id')
        | list }}

triggers:
  - trigger: state
    id: "t1"
    entity_id: !input trigger_input
    from: 'off'
    to: 'on'
  - trigger: state
    id: "t2"
    entity_id: !input trigger_input
    from: 'on'
    to: 'off'
  - trigger: state
    entity_id: !input timer_helper
    to: "idle"
    id: 't3'
  - trigger: state
    id: "t4"
    entity_id: !input motion_trigger
    from: "off"
    to: "on"
  - trigger: state
    id: "t5_on"
    entity_id: !input bypass_lights_on
    from: "off"
    to: "on"
  - trigger: state
    id: "t5_off"
    entity_id: !input bypass_lights_off
    from: "off"
    to: "on"
  - trigger: state
    id: "t5_stop"
    entity_id: !input bypass_lights_stop
    from: "off"
    to: "on"
  - trigger: state
    id: "t6_on"
    entity_id: !input bypass_lights_on
    from: "on"
    to: "off"
  - trigger: state
    id: "t6_off"
    entity_id: !input bypass_lights_off
    from: "on"
    to: "off"
  - trigger: state
    id: "t6_stop"
    entity_id: !input bypass_lights_stop
    from: "on"
    to: "off"
  - trigger: homeassistant
    id: "t7"
    event: start

# Все условия
condition:
# Условия триггеров
  - condition: or
    conditions:
      - condition: and # Стартовый триггер
        conditions:
          - condition: trigger
            id: 't1'
          - condition: state
            entity_id: !input trigger_input
            match: any
            state: 'on'
          - alias: "Проверить, включено ли состояние объекта ночного освещения"
            condition: or
            conditions:
              - condition: and
                conditions:
                  - "{{ include_grace_period == 'enable_grace_period' }}"
                  - condition: state
                    entity_id: !input grace_period_timer_helper
                    state: 'idle'
              - condition: template
                value_template: "{{ include_grace_period == 'disable_grace_period' }}"
              - condition: template
                value_template: "{{ trigger.to_state.context.parent_id is none }}"
      - condition: and # Конечный триггер
        conditions:
          - condition: trigger
            id: 't2'
          - condition: state
            entity_id: !input trigger_input
            match: all
            state: 'off'
      - condition: and # Триггер синхронизации
        conditions:
          - condition: trigger
            id: 't2'
          - condition: template
            value_template: "{{include_trigger_sync == 'enable_trigger_sync'}}"
      - condition: and # Завершение помощника-таймера
        conditions:
          - condition: trigger
            id: 't3'
          - condition: template
            value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
          - condition: template
            value_template: "{{ expand(trigger_input) | selectattr('state', '==', 'on') | list | count > 0 }}"
      - condition: and # Триггер датчика движения
        conditions:
          - condition: trigger
            id: 't4'
          - "{{include_motion_trigger == 'enable_motion_trigger'}}"
          - alias: "Проверить, включен ли период ожидания"
            condition: or
            conditions:
              - condition: and
                conditions:
                  - "{{ expand(trigger_input) | selectattr('state', '==', 'on') | list | count > 0 }}"
              - condition: and
                conditions:
                  - condition: template
                    value_template: "{{ include_grace_period == 'enable_grace_period' }}"
                  - condition: state
                    entity_id: !input grace_period_timer_helper
                    state: 'active'
      - condition: and # Триггер включения обхода
        conditions:
          - condition: trigger
            id: 't5_on'
          - "{{ 'bypass_enabled_turn_on' in include_bypass }}"
      - condition: and # Триггер включения обхода
        conditions:
          - condition: trigger
            id: 't5_off'
          - "{{ 'bypass_enabled_turn_off' in include_bypass }}"
      - condition: and # Триггер остановки обхода
        conditions:
          - condition: trigger
            id: 't5_stop'
          - "{{ 'bypass_enabled_stop' in include_bypass }}"
      - condition: and # Триггер выключения обхода
        conditions:
          - condition: trigger
            id: 't6_on'
          - "{{ 'bypass_enabled_turn_on' in include_bypass }}"
      - condition: and # Триггер выключения обхода
        conditions:
          - condition: trigger
            id: 't6_off'
          - "{{ 'bypass_enabled_turn_off' in  include_bypass }}"
      - condition: and # Триггер выключения обхода остановки
        conditions:
          - condition: trigger
            id: 't6_stop'
          - "{{ 'bypass_enabled_stop' in include_bypass }}"
      - condition: and # Триггер перезагрузки HA, проверка включенных светильников
        conditions:
          - condition: trigger
            id: 't7'
          - condition: template
            value_template: "{{ expand(trigger_input) | selectattr('state', '==', 'on') | list | count > 0 }}"
      - condition: and # Триггер перезагрузки HA и проверка включенного автовыключения обхода
        conditions:
          - condition: trigger
            id: 't7'
          - "{{ ('bypass_enabled_turn_on' in include_bypass) or ('bypass_enabled_turn_off' in include_bypass) or ('bypass_enabled_stop' in include_bypass) }}"
          - "{{ ('bypass_auto_off_enabled_on' in include_bypass_auto_off) or ('bypass_auto_off_enabled_off' in include_bypass_auto_off) or ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
          - condition: or
            conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'

# Проверка ручного обхода датчика движения
  - condition: or
    conditions:
      - "{{ include_bypass == [] }}"
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_on
            state: 'off'
          - condition: state
            entity_id: !input bypass_lights_off
            state: 'off'
          - condition: state
            entity_id: !input bypass_lights_stop
            state: 'off'
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_on
            state: 'off'
          - condition: state
            entity_id: !input bypass_lights_off
            state: 'off'
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_on
            state: 'off'
          - condition: state
            entity_id: !input bypass_lights_stop
            state: 'off'
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_off
            state: 'off'
          - condition: state
            entity_id: !input bypass_lights_stop
            state: 'off'
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_on
            state: 'off'
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_off
            state: 'off'
      - condition: and
        conditions:
          - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
          - condition: state
            entity_id: !input bypass_lights_stop
            state: 'off'
      - condition: trigger
        id: 
          - 't5_on'
          - 't5_off'
          - 't5_stop'
          - 't6_on'
          - 't6_off'
          - 't6_stop'
          - 't7'

action:
  - choose:
    - alias: "Обход включен и проверка опции обхода - Включить светильники"
      conditions:
        - condition: trigger
          id: 't5_on'
      sequence:
        - choose:
          - alias: "Если выбран период ожидания"
            conditions:
              - condition: template
                value_template: "{{include_grace_period == 'enable_grace_period'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
              - condition: state
                entity_id: !input grace_period_timer_helper
                state: 'active'
            sequence:
              - alias: "Отменить помощника-таймера"
                action: timer.cancel
                entity_id: !input grace_period_timer_helper
        - alias: "Включить объекты"
          action: homeassistant.turn_on
          entity_id: !input trigger_input
        - alias: "Проверить настройки обхода и выполнить правильное действие"
          if:
            - condition: or
              conditions:
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_on' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_on
                      match: any
                      state: 'on'
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_off' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_off
                      match: any
                      state: 'on'
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_stop' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_stop
                      match: any
                      state: 'on'
          then:
            - choose:
              - alias: "Проверить, включен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                sequence:
                  - alias: "Включить помощника-таймера"
                    action: timer.start
                    entity_id: !input timer_helper
                    data:
                      duration:
                        minutes: !input bypass_auto_off_delay
                  - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                    wait_for_trigger:
                      - trigger: event
                        event_type: timer.finished
                        event_data:
                          entity_id: !input timer_helper
              - alias: "Проверить, отключен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                sequence:
                  - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                    delay:
                      minutes: !input bypass_auto_off_delay
            - alias: "Параллельные действия для автовыключения обхода"
              parallel:
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_auto_off_enabled_on' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_on
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_turn_off' in include_bypass) and ('bypass_auto_off_enabled_off' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_off
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_stop' in include_bypass) and ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_stop
            - stop: "Остановить автоматизацию"
          else:
            - choose:
              - alias: "Проверить, включен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                sequence:
                  - alias: "Отменить помощника-таймера"
                    action: timer.cancel
                    entity_id: !input timer_helper
            - stop: "Остановить автоматизацию"
    - alias: "Обход включен и проверка опции обхода - Выключить светильники"
      conditions:
        - condition: trigger
          id: 't5_off'
      sequence:
        - choose:
          - alias: "Если выбран период ожидания"
            conditions:
              - condition: template
                value_template: "{{include_grace_period == 'enable_grace_period'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
              - condition: state
                entity_id: !input grace_period_timer_helper
                state: 'active'
            sequence:
              - alias: "Отменить помощника-таймера"
                action: timer.cancel
                entity_id: !input grace_period_timer_helper
        - choose:
          - alias: "Проверить, включены ли какие-либо светильники"
            conditions:
              - condition: state
                entity_id: !input trigger_input
                match: any
                state: 'on'
            sequence:
              - choose:
                - alias: "Проверить, включен ли помощник-таймер"
                  conditions:
                    - condition: template
                      value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                  sequence:
                    - alias: "Обработка помощника-таймера при установке в 0"
                      choose:
                        - conditions: "{{ bypass_time_delay | float == 0 }}"
                          sequence:
                            - alias: "Отменить таймер, если установлен в 0"
                              action: timer.cancel
                              entity_id: !input timer_helper
                      default:
                        - alias: "Запустить таймер с установленными минутами"
                          action: timer.start
                          entity_id: !input timer_helper
                          data:
                            duration:
                              minutes: !input bypass_time_delay
                    - alias: "Ожидать количество минут, установленное во временной задержке обхода"
                      wait_for_trigger:
                        - trigger: event
                          event_type: timer.finished
                          event_data:
                            entity_id: !input timer_helper
                - alias: "Проверить, отключен ли помощник-таймер"
                  conditions:
                    - condition: template
                      value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                  sequence:
                    - alias: "Ожидать количество минут, установленное во временной задержке обхода"
                      delay:
                        minutes: !input bypass_time_delay
              - alias: "Выключить объекты"
                action: homeassistant.turn_off
                entity_id: !input trigger_input
        - alias: "Проверить настройки обхода и выполнить правильное действие"
          if:
            - condition: or
              conditions:
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_on' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_on
                      match: any
                      state: 'on'
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_off' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_off
                      match: any
                      state: 'on'
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_stop' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_stop
                      match: any
                      state: 'on'
          then:
            - choose:
              - alias: "Проверить, включен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                sequence:
                  - alias: "Включить помощника-таймера"
                    action: timer.start
                    entity_id: !input timer_helper
                    data:
                      duration:
                        minutes: !input bypass_auto_off_delay
                  - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                    wait_for_trigger:
                      - trigger: event
                        event_type: timer.finished
                        event_data:
                          entity_id: !input timer_helper
              - alias: "Проверить, отключен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                sequence:
                  - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                    delay:
                      minutes: !input bypass_auto_off_delay
            - alias: "Параллельные действия для автовыключения обхода"
              parallel:
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_auto_off_enabled_on' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_on
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_turn_off' in include_bypass) and ('bypass_auto_off_enabled_off' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_off
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_stop' in include_bypass) and ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_stop
            - stop: "Остановить автоматизацию"
          else:
            - choose:
              - alias: "Проверить, включен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                sequence:
                  - alias: "Отменить помощника-таймера"
                    action: timer.cancel
                    entity_id: !input timer_helper
            - stop: "Остановить автоматизацию"
    - alias: "Обход включен и проверка опции обхода - Сохранить текущее состояние светильников"
      conditions:
        - condition: trigger
          id: 't5_stop'
      sequence:
        - choose:
          - alias: "Проверить, включен ли помощник-таймер"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
            sequence:
              - choose:
                - alias: "Если выбран период ожидания"
                  conditions:
                    - condition: template
                      value_template: "{{include_grace_period == 'enable_grace_period'}}"
                    - condition: template
                      value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                    - condition: state
                      entity_id: !input grace_period_timer_helper
                      state: 'active'
                  sequence:
                    - alias: "Отменить помощника-таймера"
                      action: timer.cancel
                      entity_id: !input grace_period_timer_helper
          - alias: "Проверить, отключен ли помощник-таймер"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
            sequence:
              - choose:
                - alias: "Если выбран период ожидания"
                  conditions:
                    - condition: template
                      value_template: "{{include_grace_period == 'enable_grace_period'}}"
                    - condition: template
                      value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                    - condition: state
                      entity_id: !input grace_period_timer_helper
                      state: 'active'
                  sequence:
                    - alias: "Отменить помощника-таймера"
                      action: timer.cancel
                      entity_id: !input grace_period_timer_helper
        - alias: "Проверить настройки обхода и выполнить правильное действие"
          if:
            - condition: or
              conditions:
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_on' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_on
                      match: any
                      state: 'on'
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_off' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_off
                      match: any
                      state: 'on'
                - condition: and
                  conditions:
                    - alias: "Проверить, включено ли автовыключение обхода"
                      condition: template
                      value_template: "{{ 'bypass_auto_off_enabled_stop' in include_bypass_auto_off }}"
                    - condition: state
                      entity_id: !input bypass_lights_stop
                      match: any
                      state: 'on'
          then:
            - choose:
              - alias: "Проверить, включен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                sequence:
                  - alias: "Включить помощника-таймера"
                    action: timer.start
                    entity_id: !input timer_helper
                    data:
                      duration:
                        minutes: !input bypass_auto_off_delay
                  - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                    wait_for_trigger:
                      - trigger: event
                        event_type: timer.finished
                        event_data:
                          entity_id: !input timer_helper
              - alias: "Проверить, отключен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                sequence:
                  - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                    delay:
                      minutes: !input bypass_auto_off_delay
            - alias: "Параллельные действия для автовыключения обхода"
              parallel:
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_auto_off_enabled_on' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_on
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_turn_off' in include_bypass) and ('bypass_auto_off_enabled_off' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_off
                - sequence:
                    - choose:
                        - conditions:
                            - condition: template
                              value_template: "{{ ('bypass_enabled_stop' in include_bypass) and ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
                          sequence:
                            - alias: "Выключить обход"
                              action: homeassistant.turn_off
                              entity_id: !input bypass_lights_stop
            - stop: "Остановить автоматизацию"
          else:
            - choose:
              - alias: "Проверить, включен ли помощник-таймер"
                conditions:
                  - condition: template
                    value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                sequence:
                  - alias: "Отменить помощника-таймера"
                    action: timer.cancel
                    entity_id: !input timer_helper
            - stop: "Остановить автоматизацию"
  - choose:
    - alias: "Обход выключен и проверка включенных светильников"
      conditions:
        - condition: trigger
          id:
            - 't6_on'
            - 't6_off'
            - 't6_stop'
        - condition: state
          entity_id: !input trigger_input
          match: any
          state: 'on'
      sequence:
        - choose:
          - alias: "Проверить, все ли обходы выключены и условия включены"
            conditions:
              - condition: or
                conditions:
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        state: 'off'
                      - condition: state
                        entity_id: !input bypass_lights_off
                        state: 'off'
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        state: 'off'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        state: 'off'
                      - condition: state
                        entity_id: !input bypass_lights_off
                        state: 'off'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        state: 'off'
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        state: 'off'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_off
                        state: 'off'
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        state: 'off'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        state: 'off'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_off
                        state: 'off'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        state: 'off'
            sequence:
              - choose:
                - alias: "Проверить, включен ли помощник-таймер"
                  conditions:
                    - condition: template
                      value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                  sequence:
                    - choose:
                      - alias: "Если выбран период ожидания"
                        conditions:
                          - condition: template
                            value_template: "{{include_grace_period == 'enable_grace_period'}}"
                          - condition: template
                            value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                        sequence:
                          - choose:
                            - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                              conditions:
                                - condition: template
                                  value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                                - condition: template
                                  value_template: "{{ trigger_input | length > 1 }}"
                              sequence:
                                - action: input_text.set_value
                                  target:
                                    entity_id: "{{ saved_lights_input }}"
                                  data:
                                    value: "{{ trigger_entities_on | join(', ') }}"
                          - choose:
                            - alias: "Если таймер работает"
                              conditions:
                                - condition: state
                                  entity_id: !input grace_period_timer_helper
                                  state: ['active', 'paused']
                              sequence:
                                - alias: "Отменить помощника-таймера"
                                  action: timer.cancel
                                  entity_id: !input grace_period_timer_helper
                    - choose:
                      - alias: "Если выбрана синхронизация триггеров"
                        conditions:
                          - condition: template
                            value_template: "{{include_trigger_sync == 'enable_trigger_sync'}}"
                        sequence:
                          - alias: "Включить объекты"
                            action: homeassistant.turn_on
                            target:
                              entity_id: "{{ trigger_entities_off }}"
                    - alias: "Обработка помощника-таймера при установке в 0"
                      choose:
                        - conditions: "{{ bypass_time_delay | float == 0 }}"
                          sequence:
                            - alias: "Отменить таймер, если установлен в 0"
                              action: timer.cancel
                              entity_id: !input timer_helper
                      default:
                        - alias: "Запустить таймер с установленными минутами"
                          action: timer.start
                          entity_id: !input timer_helper
                          data:
                            duration: 
                              minutes: !input bypass_time_delay
                    - choose:
                      - alias: "Проверить, включен ли триггер движения и активен"
                        conditions:
                          - condition: template
                            value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                          - condition: state
                            entity_id: !input motion_trigger
                            state: 'on'
                        sequence:
                          - alias: "Приостановить помощника-таймера"
                            action: timer.pause
                            entity_id: !input timer_helper
                          - alias: "Ожидать, пока датчик движения выключится"
                            wait_for_trigger:
                              trigger: state
                              entity_id: !input motion_trigger
                              from: "on"
                              to: "off"
                          - alias: "Включить помощника-таймера"
                            action: timer.start
                            entity_id: !input timer_helper
                            data:
                              duration: !input time_delay
                - alias: "Проверить, отключен ли помощник-таймер"
                  conditions:
                    - condition: template
                      value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                  sequence:
                    - choose:
                      - alias: "Если выбран период ожидания"
                        conditions:
                          - condition: template
                            value_template: "{{include_grace_period == 'enable_grace_period'}}"
                          - condition: template
                            value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                        sequence:
                          - choose:
                            - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                              conditions:
                                - condition: template
                                  value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                                - condition: template
                                  value_template: "{{ trigger_input | length > 1 }}"
                              sequence:
                                - action: input_text.set_value
                                  target:
                                    entity_id: "{{ saved_lights_input }}"
                                  data:
                                    value: "{{ trigger_entities_on | join(', ') }}"
                          - choose:
                            - alias: "Если таймер работает"
                              conditions:
                                - condition: state
                                  entity_id: !input grace_period_timer_helper
                                  state: ['active', 'paused']
                              sequence:
                                - alias: "Отменить помощника-таймера"
                                  action: timer.cancel
                                  entity_id: !input grace_period_timer_helper
                    - choose:
                      - alias: "Если выбрана синхронизация триггеров"
                        conditions:
                          - condition: template
                            value_template: "{{include_trigger_sync == 'enable_trigger_sync'}}"
                        sequence:
                          - alias: "Включить объекты"
                            action: homeassistant.turn_on
                            target:
                              entity_id: "{{ trigger_entities_off }}"
                    - choose:
                      - alias: "Проверить, включен ли триггер движения и активен"
                        conditions:
                          - condition: template
                            value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                          - condition: state
                            entity_id: !input motion_trigger
                            state: 'on'
                        sequence:
                          - alias: "Ожидать, пока датчик движения выключится"
                            wait_for_trigger:
                              trigger: state
                              entity_id: !input motion_trigger
                              from: "on"
                              to: "off"
                    - alias: "Ожидать время, установленное во временной задержке"
                      delay:
                        minutes: !input bypass_time_delay
                    - choose:
                      - alias: "Если выбран период ожидания"
                        conditions:
                          - condition: template
                            value_template: "{{include_grace_period == 'enable_grace_period'}}"
                          - condition: template
                            value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                        sequence:
                          - alias: "Включить помощника-таймера"
                            action: timer.start
                            entity_id: !input grace_period_timer_helper
                            data:
                              duration: !input grace_period_time_delay
                    - alias: "Выключить объекты"
                      action: homeassistant.turn_off
                      target:
                        entity_id: "{{ trigger_entities_on }}"
              - stop: "Остановить автоматизацию"
          - alias: "Проверить, включены ли какие-либо обходы"
            conditions:
              - condition: or
                conditions:
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        match: any
                        state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_off
                        match: any
                        state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        match: any
                        state: 'on'
            sequence:
              - alias: "Проверить настройки обхода и выполнить правильное действие"
                if:
                  - condition: or
                    conditions:
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_on' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_off' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_stop' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                then:
                  - choose:
                    - alias: "Проверить, включен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                      sequence:
                        - alias: "Включить помощника-таймера"
                          action: timer.start
                          entity_id: !input timer_helper
                          data:
                            duration:
                              minutes: !input bypass_auto_off_delay
                        - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                          wait_for_trigger:
                            - trigger: event
                              event_type: timer.finished
                              event_data:
                                entity_id: !input timer_helper
                    - alias: "Проверить, отключен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                      sequence:
                        - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                          delay:
                            minutes: !input bypass_auto_off_delay
                  - alias: "Параллельные действия для автовыключения обхода"
                    parallel:
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_auto_off_enabled_on' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_on
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_turn_off' in include_bypass) and ('bypass_auto_off_enabled_off' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_off
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_stop' in include_bypass) and ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_stop
                  - stop: "Остановить автоматизацию"
                else:
                  - choose:
                    - alias: "Проверить, включен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                      sequence:
                        - alias: "Отменить помощника-таймера"
                          action: timer.cancel
                          entity_id: !input timer_helper
                  - stop: "Остановить автоматизацию"
    - alias: "Обход выключен и проверка, все ли светильники выключены"
      conditions:
        - condition: trigger
          id:
            - 't6_on'
            - 't6_off'
            - 't6_stop'
        - condition: state
          entity_id: !input trigger_input
          match: all
          state: 'off'
      sequence:
        - choose:
          - alias: "Проверить, включены ли какие-либо обходы"
            conditions:
              - condition: or
                conditions:
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        match: any
                        state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_off
                        match: any
                        state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        match: any
                        state: 'on'
            sequence:
              - alias: "Проверить настройки обхода и выполнить правильное действие"
                if:
                  - condition: or
                    conditions:
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_on' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_off' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_stop' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                then:
                  - choose:
                    - alias: "Проверить, включен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                      sequence:
                        - alias: "Включить помощника-таймера"
                          action: timer.start
                          entity_id: !input timer_helper
                          data:
                            duration:
                              minutes: !input bypass_auto_off_delay
                        - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                          wait_for_trigger:
                            - trigger: event
                              event_type: timer.finished
                              event_data:
                                entity_id: !input timer_helper
                    - alias: "Проверить, отключен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                      sequence:
                        - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                          delay:
                            minutes: !input bypass_auto_off_delay
                  - alias: "Параллельные действия для автовыключения обхода"
                    parallel:
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_auto_off_enabled_on' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_on
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_turn_off' in include_bypass) and ('bypass_auto_off_enabled_off' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_off
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_stop' in include_bypass) and ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_stop
                  - stop: "Остановить автоматизацию"
                else:
                  - choose:
                    - alias: "Проверить, включен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                      sequence:
                        - alias: "Отменить помощника-таймера"
                          action: timer.cancel
                          entity_id: !input timer_helper
                  - stop: "Остановить автоматизацию"
          - alias: "Проверить, включен ли помощник-таймер"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
            sequence:
              - alias: "Отменить помощника-таймера"
                action: timer.cancel
                entity_id: !input timer_helper
    - alias: "Перезагрузка Home Assistant"
      conditions:
        - condition: trigger
          id: 't7'
      sequence:
        - choose:
          - alias: "Проверить, включены ли какие-либо обходы"
            conditions:
              - condition: or
                conditions:
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_on
                        match: any
                        state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' in include_bypass) and ('bypass_enabled_stop' not in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_off
                        match: any
                        state: 'on'
                  - condition: and
                    conditions:
                      - "{{ ('bypass_enabled_turn_on' not in include_bypass) and ('bypass_enabled_turn_off' not in include_bypass) and ('bypass_enabled_stop' in include_bypass) }}"
                      - condition: state
                        entity_id: !input bypass_lights_stop
                        match: any
                        state: 'on'
            sequence:
              - alias: "Проверить настройки обхода и выполнить правильное действие"
                if:
                  - condition: or
                    conditions:
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_on' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_on
                            match: any
                            state: 'on'
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_off' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_off
                            match: any
                            state: 'on'
                      - condition: and
                        conditions:
                          - alias: "Проверить, включено ли автовыключение обхода"
                            condition: template
                            value_template: "{{ 'bypass_auto_off_enabled_stop' in include_bypass_auto_off }}"
                          - condition: state
                            entity_id: !input bypass_lights_stop
                            match: any
                            state: 'on'
                then:
                  - choose:
                    - alias: "Проверить, включен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                      sequence:
                        - alias: "Включить помощника-таймера"
                          action: timer.start
                          entity_id: !input timer_helper
                          data:
                            duration:
                              minutes: !input bypass_auto_off_delay
                        - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                          wait_for_trigger:
                            - trigger: event
                              event_type: timer.finished
                              event_data:
                                entity_id: !input timer_helper
                    - alias: "Проверить, отключен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
                      sequence:
                        - alias: "Ожидать количество минут, установленное во временной задержке автовыключения обхода"
                          delay:
                            minutes: !input bypass_auto_off_delay
                  - alias: "Параллельные действия для автовыключения обхода"
                    parallel:
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_turn_on' in include_bypass) and ('bypass_auto_off_enabled_on' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_on
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_turn_off' in include_bypass) and ('bypass_auto_off_enabled_off' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_off
                      - sequence:
                          - choose:
                              - conditions:
                                  - condition: template
                                    value_template: "{{ ('bypass_enabled_stop' in include_bypass) and ('bypass_auto_off_enabled_stop' in include_bypass_auto_off) }}"
                                sequence:
                                  - alias: "Выключить обход"
                                    action: homeassistant.turn_off
                                    entity_id: !input bypass_lights_stop
                  - stop: "Остановить автоматизацию"
                else:
                  - choose:
                    - alias: "Проверить, включен ли помощник-таймер"
                      conditions:
                        - condition: template
                          value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                      sequence:
                        - alias: "Отменить помощника-таймера"
                          action: timer.cancel
                          entity_id: !input timer_helper
                  - stop: "Остановить автоматизацию"
        - choose:
          - alias: "Перезагрузка HA: таймер включен и движение есть"
            conditions:
              - "{{include_timer_helper == 'enable_timer_helper'}}"
              - "{{include_motion_trigger == 'enable_motion_trigger'}}"
              - condition: state
                entity_id: !input motion_trigger
                state: 'on'
            sequence:
              - choose:
                  - alias: "Проверить, приостановлен ли таймер"
                    conditions:
                      - condition: state
                        entity_id: !input timer_helper
                        state: 'paused'
                    sequence:
                      - alias: "Приостановить помощника-таймера"
                        action: timer.pause
                        entity_id: !input timer_helper
                      - alias: "Ожидать, пока датчик движения выключится"
                        wait_for_trigger:
                          trigger: state
                          entity_id: !input motion_trigger
                          from: "on"
                          to: "off"
                      - alias: "Включить помощника-таймера"
                        action: timer.start
                        entity_id: !input timer_helper
                        data:
                          duration: !input time_delay
                  - alias: "Проверить, остановлен ли таймер"
                    conditions:
                      - condition: state
                        entity_id: !input timer_helper
                        state: 'idle'
                    sequence:
                      - alias: "Выключить объекты"
                        action: homeassistant.turn_off
                        entity_id: !input trigger_input
          - alias: "Перезагрузка HA: таймер включен и движения нет"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
              - condition: state
                entity_id: !input motion_trigger
                state: 'off'
            sequence:
              - choose:
                  - alias: "Проверить, приостановлен ли таймер"
                    conditions:
                      - condition: state
                        entity_id: !input timer_helper
                        state: 'paused'
                    sequence:
                      - alias: "Включить помощника-таймера"
                        action: timer.start
                        entity_id: !input timer_helper
                        data:
                          duration: !input time_delay
                  - alias: "Проверить, остановлен ли таймер"
                    conditions:
                      - condition: state
                        entity_id: !input timer_helper
                        state: 'idle'
                    sequence:
                      - alias: "Выключить объекты"
                        action: homeassistant.turn_off
                        entity_id: !input trigger_input
          - alias: "Перезагрузка HA: таймер включен и движение отключено"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'disable_motion_trigger'}}"
            sequence:
              - choose:
                  - alias: "Проверить, приостановлен ли таймер"
                    conditions:
                      - condition: state
                        entity_id: !input timer_helper
                        state: 'paused'
                    sequence:
                      - alias: "Включить помощника-таймера"
                        action: timer.start
                        entity_id: !input timer_helper
                        data:
                          duration: !input time_delay
                  - alias: "Проверить, остановлен ли таймер"
                    conditions:
                      - condition: state
                        entity_id: !input timer_helper
                        state: 'idle'
                    sequence:
                      - alias: "Выключить объекты"
                        action: homeassistant.turn_off
                        entity_id: !input trigger_input
          - alias: "Перезагрузка HA: таймер отключен и движение есть"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
              - condition: state
                entity_id: !input motion_trigger
                state: 'on'
            sequence:
              - alias: "Ожидать, пока датчик движения выключится"
                wait_for_trigger:
                  trigger: state
                  entity_id: !input motion_trigger
                  from: "on"
                  to: "off"
              - alias: "Применить временную задержку"
                delay: !input time_delay
              - alias: "Выключить объекты"
                action: homeassistant.turn_off
                entity_id: !input trigger_input
          - alias: "Перезагрузка HA: таймер отключен и движения нет"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
              - condition: state
                entity_id: !input motion_trigger
                state: 'off'
            sequence:
              - alias: "Применить временную задержку"
                delay: !input time_delay
              - alias: "Выключить объекты"
                action: homeassistant.turn_off
                entity_id: !input trigger_input
          - alias: "Перезагрузка HA: таймер отключен и движение отключено"
            conditions:
              - condition: template
                value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
              - condition: template
                value_template: "{{include_motion_trigger == 'disable_motion_trigger'}}"
            sequence:
              - alias: "Применить временную задержку"
                delay: !input time_delay
              - alias: "Выключить объекты"
                action: homeassistant.turn_off
                entity_id: !input trigger_input
        - stop: "Остановить автоматизацию"
  - choose:
      - alias: "Триггер включен"
        conditions:
          - condition: trigger
            id: 
              - 't1'
        sequence:
          - choose:
            - alias: "Проверить, включен ли помощник-таймер"
              conditions:
                - condition: template
                  value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
              sequence:
                - choose:
                  - alias: "Если выбран период ожидания"
                    conditions:
                      - condition: template
                        value_template: "{{include_grace_period == 'enable_grace_period'}}"
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                    sequence:
                      - choose:
                        - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                          conditions:
                            - condition: template
                              value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                            - condition: template
                              value_template: "{{ trigger_input | length > 1 }}"
                          sequence:
                            - action: input_text.set_value
                              target:
                                entity_id: "{{ saved_lights_input }}"
                              data:
                                value: "{{ trigger_entities_on | join(', ') }}"
                      - choose:
                        - alias: "Если таймер работает"
                          conditions:
                            - condition: state
                              entity_id: !input grace_period_timer_helper
                              state: ['active', 'paused']
                          sequence:
                            - alias: "Отменить помощника-таймера"
                              action: timer.cancel
                              entity_id: !input grace_period_timer_helper
                - choose:
                  - alias: "Если выбрана синхронизация триггеров"
                    conditions:
                      - condition: template
                        value_template: "{{include_trigger_sync == 'enable_trigger_sync'}}"
                    sequence:
                      - alias: "Включить объекты"
                        action: homeassistant.turn_on
                        target:
                          entity_id: "{{ trigger_entities_off }}"
                - alias: "Включить помощника-таймера"
                  action: timer.start
                  entity_id: !input timer_helper
                  data:
                    duration: !input time_delay
                - choose:
                  - alias: "Проверить, включен ли триггер движения и активен"
                    conditions:
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                      - condition: state
                        entity_id: !input motion_trigger
                        state: 'on'
                    sequence:
                      - alias: "Приостановить помощника-таймера"
                        action: timer.pause
                        entity_id: !input timer_helper
                      - alias: "Ожидать, пока датчик движения выключится"
                        wait_for_trigger:
                          trigger: state
                          entity_id: !input motion_trigger
                          from: "on"
                          to: "off"
                      - alias: "Включить помощника-таймера"
                        action: timer.start
                        entity_id: !input timer_helper
                        data:
                          duration: !input time_delay
            - alias: "Проверить, отключен ли помощник-таймер"
              conditions:
                - condition: template
                  value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
              sequence:
                - choose:
                  - alias: "Если выбран период ожидания"
                    conditions:
                      - condition: template
                        value_template: "{{include_grace_period == 'enable_grace_period'}}"
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                    sequence:
                      - choose:
                        - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                          conditions:
                            - condition: template
                              value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                            - condition: template
                              value_template: "{{ trigger_input | length > 1 }}"
                          sequence:
                            - action: input_text.set_value
                              target:
                                entity_id: "{{ saved_lights_input }}"
                              data:
                                value: "{{ trigger_entities_on | join(', ') }}"
                      - choose:
                        - alias: "Если таймер работает"
                          conditions:
                            - condition: state
                              entity_id: !input grace_period_timer_helper
                              state: ['active', 'paused']
                          sequence:
                            - alias: "Отменить помощника-таймера"
                              action: timer.cancel
                              entity_id: !input grace_period_timer_helper
                - choose:
                  - alias: "Если выбрана синхронизация триггеров"
                    conditions:
                      - condition: template
                        value_template: "{{include_trigger_sync == 'enable_trigger_sync'}}"
                    sequence:
                      - alias: "Включить объекты"
                        action: homeassistant.turn_on
                        target:
                          entity_id: "{{ trigger_entities_off }}"
                - choose:
                  - alias: "Проверить, включен ли триггер движения и активен"
                    conditions:
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                      - condition: state
                        entity_id: !input motion_trigger
                        state: 'on'
                    sequence:
                      - alias: "Ожидать, пока датчик движения выключится"
                        wait_for_trigger:
                          trigger: state
                          entity_id: !input motion_trigger
                          from: "on"
                          to: "off"
                - alias: "Ожидать время, установленное во временной задержке"
                  delay: !input time_delay
                - choose:
                  - alias: "Если выбран период ожидания"
                    conditions:
                      - condition: template
                        value_template: "{{include_grace_period == 'enable_grace_period'}}"
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                    sequence:
                      - alias: "Включить помощника-таймера"
                        action: timer.start
                        entity_id: !input grace_period_timer_helper
                        data:
                          duration: !input grace_period_time_delay
                - alias: "Выключить объекты"
                  action: homeassistant.turn_off
                  target:
                    entity_id: "{{ trigger_entities_on }}"
      - alias: "Триггер выключен"
        conditions:
          - condition: trigger
            id: 't2'
        sequence:
          - choose:
              - alias: "Проверить, включен ли помощник-таймер и отменить его"
                conditions:
                  - condition: state
                    entity_id: !input trigger_input
                    match: all
                    state: 'off'
                sequence:
                  - choose:
                      - alias: "Проверить, включен ли помощник-таймер и отменить его"
                        conditions:
                          - condition: template
                            value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
                        sequence:
                          - alias: "Отменить помощника-таймера"
                            action: timer.cancel
                            entity_id: !input timer_helper
              - alias: "Проверить, включена ли синхронизация триггерных объектов и ручное выключение"
                conditions:
                  - condition: template
                    value_template: "{{include_trigger_sync == 'enable_trigger_sync'}}"
                sequence:
                  - alias: "Выключить объекты"
                    action: homeassistant.turn_off
                    target:
                      entity_id: "{{ trigger_entities_on }}"
                  - stop: "Остановить автоматизацию"
      - alias: "Выключить объекты при завершении помощника-таймера"
        conditions:
          - condition: trigger
            id: 't3'
        sequence:
          - choose:
            - alias: "Если выбран период ожидания"
              conditions:
                - condition: template
                  value_template: "{{include_grace_period == 'enable_grace_period'}}"
                - condition: template
                  value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
              sequence:
                - alias: "Включить помощника-таймера"
                  action: timer.start
                  entity_id: !input grace_period_timer_helper
                  data:
                    duration: !input grace_period_time_delay
                - choose:
                  - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                    conditions:
                      - condition: template
                        value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                      - condition: template
                        value_template: "{{ trigger_input | length > 1 }}"
                    sequence:
                      - action: input_text.set_value
                        data:
                          entity_id: "{{ saved_lights_input }}"
                          value: "{{ trigger_entities_on | join(', ') }}"
          - alias: "Выключить объекты"
            action: homeassistant.turn_off
            target:
              entity_id: "{{ trigger_entities_on }}"
      - alias: "Обнаружено движение"
        conditions:
          - condition: trigger
            id: 't4'
        sequence:
          - choose:
            - alias: "Проверить, включен ли помощник-таймер"
              conditions:
                - condition: template
                  value_template: "{{include_timer_helper == 'enable_timer_helper'}}"
              sequence:
                - choose:
                  - alias: "Если выбран период ожидания"
                    conditions:
                      - condition: template
                        value_template: "{{include_grace_period == 'enable_grace_period'}}"
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                      - condition: state
                        entity_id: !input grace_period_timer_helper
                        state: 'active'
                    sequence:
                      - choose:
                        - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                          conditions:
                            - condition: template
                              value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                            - condition: template
                              value_template: "{{ trigger_input | length > 1 }}"
                          sequence:
                            - action: homeassistant.turn_on
                              target:
                                entity_id: >
                                  {% set entities = states(saved_lights_input) %}
                                  {% set cleaned_entities = entities.split(', ') %}
                                  {% set cleaned_entities = cleaned_entities | map('trim') | selectattr('strip', 'ne', '') | list %}
                                  {{ cleaned_entities }}
                        - alias: "Проверить, включена ли синхронизация триггерных объектов или только один объект в триггере"
                          conditions:
                            - condition: template
                              value_template: "{{ (include_trigger_sync == 'enable_trigger_sync') or (trigger_input | length == 1) }}"
                          sequence:
                            - alias: "Включить объекты"
                              action: homeassistant.turn_on
                              target:
                                entity_id: "{{ trigger_entities_off }}"
                      - alias: "Отменить помощника-таймера"
                        action: timer.cancel
                        entity_id: !input grace_period_timer_helper
                - alias: "Приостановить помощника-таймера"
                  action: timer.pause
                  entity_id: !input timer_helper
                - alias: "Ожидать, пока датчик движения выключится"
                  wait_for_trigger:
                    trigger: state
                    entity_id: !input motion_trigger
                    from: "on"
                    to: "off"
                - alias: "Включить помощника-таймера"
                  action: timer.start
                  entity_id: !input timer_helper
                  data:
                    duration: !input time_delay
            - alias: "Проверить, отключен ли помощник-таймер"
              conditions:
                - condition: template
                  value_template: "{{include_timer_helper == 'disable_timer_helper'}}"
              sequence:
                - choose:
                  - alias: "Если выбран период ожидания"
                    conditions:
                      - condition: template
                        value_template: "{{include_grace_period == 'enable_grace_period'}}"
                      - condition: template
                        value_template: "{{include_motion_trigger == 'enable_motion_trigger'}}"
                      - condition: state
                        entity_id: !input grace_period_timer_helper
                        state: 'active'
                    sequence:
                      - choose:
                        - alias: "Проверить, отключена ли синхронизация триггерных объектов и более одного объекта в триггере"
                          conditions:
                            - condition: template
                              value_template: "{{include_trigger_sync == 'disable_trigger_sync'}}"
                            - condition: template
                              value_template: "{{ trigger_input | length > 1 }}"
                          sequence:
                            - action: homeassistant.turn_on
                              target:
                                entity_id: >
                                  {% set entities = states(saved_lights_input) %}
                                  {% set cleaned_entities = entities.split(', ') %}
                                  {% set cleaned_entities = cleaned_entities | map('trim') | selectattr('strip', 'ne', '') | list %}
                                  {{ cleaned_entities }}
                        - alias: "Проверить, включена ли синхронизация триггерных объектов или только один объект в триггере"
                          conditions:
                            - condition: template
                              value_template: "{{ (include_trigger_sync == 'enable_trigger_sync') or (trigger_input | length == 1) }}"
                          sequence:
                            - alias: "Включить объекты"
                              action: homeassistant.turn_on
                              target:
                                entity_id: "{{ trigger_entities_off }}"
                      - alias: "Отменить помощника-таймера"
                        action: timer.cancel
                        entity_id: !input grace_period_timer_helper
                - alias: "Ожидать, пока датчик движения выключится"
                  wait_for_trigger:
                    trigger: state
                    entity_id: !input motion_trigger
                    from: "on"
                    to: "off"
                - alias: "Ожидать установленное время"
                  delay: !input time_delay
                - alias: "Выключить объекты"
                  action: homeassistant.turn_off
                  entity_id: !input trigger_input